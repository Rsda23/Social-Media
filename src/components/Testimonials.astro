---
import testimonials from '../data/testimonials.json';
---

<section class="py-24 bg-linear-to-br from-purple-50 to-pink-50">
  <div class="container mx-auto px-6">
    <div class="text-center max-w-3xl mx-auto mb-16 animate-on-scroll">
      <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-4">
        {testimonials.title}
      </h2>
      <p class="text-xl text-gray-600">
        {testimonials.description}
      </p>
    </div>

    <div class="grid md:grid-cols-3 gap-8">
      {testimonials.items.map((testimonial, index) => (
        <div 
          class="bg-white rounded-2xl p-8 shadow-lg hover:shadow-2xl transition-all duration-300 animate-on-scroll"
          style={`animation-delay: ${index * 0.15}s;`}
        >
          <div class="flex items-center gap-4 mb-6">
            <div class="text-5xl">{testimonial.avatar}</div>
            <div>
              <div class="font-bold text-gray-900">{testimonial.name}</div>
              <div class="text-sm text-gray-600">{testimonial.role}</div>
            </div>
          </div>
          <p class="text-gray-600 leading-relaxed italic">
            "{testimonial.content}"
          </p>
          <div class="flex gap-1 mt-6 text-yellow-400 text-xl">
            ★★★★★
          </div>
        </div>
      ))}
    </div>
  </div>
</section>